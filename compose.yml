services:
  pg:
    image: postgres:16-alpine
    ports:
      - 5432:5432
    container_name: pg
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: seminar
    networks:
      - seminar

networks:
  seminar:
    name: seminar